/** Maximum complete file size accepted by read/edit tools. */
export const MAX_TOOL_FILE_BYTES = 16 * 1024 * 1024;

/** Per-file cap for the JavaScript grep fallback. */
export const MAX_GREP_FALLBACK_FILE_BYTES = 2 * 1024 * 1024;
