interface:
  display_name: "cumcm-draft"
  short_description: "Design, solve, audit, or complete a CUMCM project"
  default_prompt: "Use $cumcm-draft and honor my requested phase boundary. Ask which Python or Conda interpreter to use before execution, read every relevant problem attachment, preserve prescribed output filenames/schemas/encoding, build responsibility-based qN code with shared logic only under code/always, checkpoint expensive work under .cumcm, atomically publish minimal validated results, and visually inspect paper-ready Chinese PNG figures. Generate or package the Chinese paper only when I request an end-to-end delivery."
policy:
  allow_implicit_invocation: false
